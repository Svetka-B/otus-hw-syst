# ansible/ansible.cfg

[defaults]

inventory = ./inventories/dev/hosts.ini
roles_path = ./roles
remote_user = ubuntu
host_key_checking = False
pipelining = True
forks = 5

[privilege_escalation]

become = True
become_method = sudo
become_user = root
become_ask_pass = False
